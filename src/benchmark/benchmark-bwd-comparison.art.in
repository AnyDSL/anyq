

#[export]
fn run(device: i32, num_threads_min: i32, num_threads_max: i32, block_size: i32, _p_enq: f32, _p_deq: f32, _workload_size: i32) {
	run_queue_benchmark(
		device,
		num_threads_min,
		num_threads_max,
		bwd_comparison_benchmark(device, block_size, 20, @queue_size@, "@queue_short_name@", @queue_constructor_name@))
}
